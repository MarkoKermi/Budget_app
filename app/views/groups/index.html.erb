<%= render partial:'/groups/shared/nav_page', locals: {title: 'Add a payments', backurl: user_groups_path(user_id:current_user.id)}  %>

<%= 'Categories'%>


<%= link_to 'Add new category', new_user_group_path(user_id: current_user.id) %>

